import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "413OPENCOURT — Elite Basketball Training & Coaching",
  description: "Train like a pro with 413OPENCOURT. Access NBA-style drills, pro training videos, progress tracking, and expert coaching. Start your free trial or get your free shooting guide.",
  openGraph: {
    title: "413OPENCOURT — Elite Basketball Training",
    description: "Train like NBA. Professional basketball coaching platform with pro-level drills and progress tracking.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
